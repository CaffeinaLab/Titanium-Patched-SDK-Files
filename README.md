# Titanium SDK patches

To install:

`wget https://github.com/caffeinalab/titanium-patched-sdk-files/archive/master.zip -O /tmp/tisdkp.zip && unzip /tmp/tisdkp.zip && /tmp/titanium-patched-sdk-files-master/patch.sh`
